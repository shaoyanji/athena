# Athena\n\nAthena is a portable, operator-legible control plane for local context, startup views, and workspace materialization.\n\nRight now, this repo is no longer just a seed. It has a real live plane shape:\n\n- real: authored Nix structure, profile surface, control docs, public GitHub flake, workspace-root materialization path\n- not yet real: full runtime/bootstrap replacement for the wider system\n\n## Canonical source and live target\n\nCanonical portable source:\n\n```bash\ngithub:shaoyanji/athena\n```\n\nCanonical local working root:\n\n```bash\ncd /var/lib/nullclaw/workspace/projects/athena\n```\n\nCanonical live target:\n\n```bash\n/var/lib/nullclaw/workspace\n```\n\n## Current working commands\n\n### Enter the dev shell\n\nFrom the repo:\n\n```bash\nnix develop\n```\n\nFrom a fresh workspace wrapper:\n\n```bash\nnix flake update\nnix develop\n```\n\n### Materialize Athena into the live workspace surface\n\nFrom the repo:\n\n```bash\nnix develop -c task -t taskfiles/Taskfile.yml activate\n```\n\nFrom the public flake directly:\n\n```bash\nnix develop github:shaoyanji/athena --command athena-activate\n```\n\nThis currently materializes live workspace links for:\n\n- `AGENTS.md`\n- `SOUL.md`\n- `USER.md`\n- `ATHENA.md`\n- `seed.json`\n- `registry/effective/registry.json`\n\n### Run repo health checks\n\n```bash\nnix develop -c task -t taskfiles/Taskfile.yml check\n```\n\n## Source of truth\n\nAuthored truth lives in:\n\n- `flake.nix`\n- `nix/default.nix`\n- `nix/registry/*`\n- `nix/taskfiles/*`\n- `src/*`\n\nGenerated files and workspace-root links are projections of that authored truth, not primary authored state.\n\n## Honest status\n\nAthena now has a real live control-plane path:\n\n- canonical GitHub flake source\n- profile-driven dev shell surface\n- store-backed workspace materialization\n- artifact-backed verification via live symlink targets\n\nBut Athena is still not yet a full runtime replacement for the wider system.\nThat distinction should remain explicit until Athena owns a real startup/bootstrap contract with proven consumers.\n\n## Next threshold\n\nTo become a full runtime replacement, Athena still needs:\n\n1. a real startup/bootstrap contract\n2. a defined operator-facing surface beyond raw materialization\n3. clear switch criteria for what it replaces\n4. verification that the surrounding system actually consumes Athena outputs\n5. first-class profile, package, tool, and skill surfaces wired through the registry\n