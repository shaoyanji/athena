{ identity, base, startupViews, profiles, packages, tools, skills }:
{
  workspace = identity;
  registryVersion = 1;
  entries = base;
  startupViews = startupViews;
  profiles = profiles;
  packages = packages;
  tools = tools;
  skills = skills;
}
