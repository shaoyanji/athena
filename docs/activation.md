# Athena Materialization\n\n## What this command really does\n\nCurrent repo command:\n\n```bash\ncd /var/lib/nullclaw/workspace/projects/athena\nnix develop -c task -t taskfiles/Taskfile.yml activate\n```\n\nEquivalent public-flake command:\n\n```bash\nnix develop github:shaoyanji/athena --command athena-activate\n```\n\nThis is now a real live materialization path.\nIt places the Athena control surface into the live workspace root as store-backed symlinks.\n\n## Outputs\n\nThe command currently materializes live workspace links for:\n\n- `AGENTS.md`\n- `SOUL.md`\n- `USER.md`\n- `ATHENA.md`\n- `seed.json`\n- `registry/effective/registry.json`\n\n## Why it exists\n\nAthena keeps authored truth small and mostly Nix-authored.\nSome consumers need concrete files at the live workspace surface, so this step materializes those projections into the workspace root.\n\n## Verification\n\n```bash\nls -l \\\n  /var/lib/nullclaw/workspace/AGENTS.md \\\n  /var/lib/nullclaw/workspace/SOUL.md \\\n  /var/lib/nullclaw/workspace/USER.md \\\n  /var/lib/nullclaw/workspace/ATHENA.md \\\n  /var/lib/nullclaw/workspace/seed.json \\\n  /var/lib/nullclaw/workspace/registry/effective/registry.json\n```\n\n```bash\nnix develop -c task -t taskfiles/Taskfile.yml check\n```\n\n## Not yet true\n\nThis step still does not by itself:\n\n- define the total wider-system startup contract\n- register Athena as the sole system entrypoint\n- prove that every surrounding consumer now trusts Athena outputs\n- replace every wider bootstrap or steering path\n- flip production traffic in a complete runtime sense\n\n## What would make it a full system switch\n\nA future full activation path would need to:\n\n1. define the startup contract\n2. place the right files in the right consuming locations\n3. verify the surrounding system uses them\n4. expose a trustworthy operator-facing switch path\n5. connect profiles, packages, tools, and skills through a real registry contract\n